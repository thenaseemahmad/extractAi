import { googleGeminiEngine } from './google-gemini-engine';

describe('googleGeminiEngine', () => {
  it('should work', () => {
    expect(googleGeminiEngine()).toEqual('google-gemini-engine');
  });
});
