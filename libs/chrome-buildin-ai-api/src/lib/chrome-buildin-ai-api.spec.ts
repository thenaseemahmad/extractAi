import { chromeBuildinAiApi } from './chrome-buildin-ai-api';

describe('chromeBuildinAiApi', () => {
  it('should work', () => {
    expect(chromeBuildinAiApi()).toEqual('chrome-buildin-ai-api');
  });
});
